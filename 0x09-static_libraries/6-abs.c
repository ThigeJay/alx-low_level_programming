#include "main.h"

/**
 * _abs - calculates the absolute value of an integer
 * @n: the integer to check on
 * Return: the value of the integer
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
